{
  "ignoreCommand": "[ \"$VERCEL_ENV\" = \"production\" ] || exit 0; cd \"$(git rev-parse --show-toplevel)\" && git diff --quiet HEAD^ HEAD -- site",
  "headers": [
    {
      "source": "/fonts/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    }
  ]
}
